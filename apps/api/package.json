{
    "name": "@talently/api",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "php artisan serve",
        "build": "php artisan config:cache",
        "lint": "./vendor/bin/pint --test",
        "test": "php artisan test",
        "setup:env": "node setup.js",
        "setup:env:force": "node setup.js --force",
        "setup": "npm run setup:env && composer install && php artisan migrate"
    }
}
